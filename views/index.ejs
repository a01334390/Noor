<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>MY APP</title>
</head>
<body>
  May Node and Express be with you.

  <ul class="project">
  <% for(var i=0; i<projects.length; i++) {%>
    <li class="project">
      <span><%= projects[i]._id%></span>
      <span><%= projects[i].name %></span>
      <span><%= projects[i].description %></span>
      <span><a href="/project?name=<%= projects[i].name%>"> see </a></span>
    </li>
  <% } %>
  </ul>
</body>
</html>