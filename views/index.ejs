<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>MY APP</title>
</head>
<body>
  May Node and Express be with you.

  <ul class="project">
  <% for(var i=0; i<projects.length; i++) {%>
    <li class="project">
     <form action="/project/<%=projects[i]._id%>">
      <span><%= projects[i]._id%></span>
      <span><%= projects[i].name %></span>
      <span><%= projects[i].description %></span>
      <button action="submit">see</button>
    </form>
    </li>
  <% } %>
  </ul>
</body>
</html>